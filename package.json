{
  "name": "claudebox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node --experimental-strip-types --no-warnings --import ./tests/setup.ts --test 'tests/libclaudebox/**/*.test.ts' 'tests/integration/**/*.test.ts'",
    "test:security": "CLAUDEBOX_SECURITY_TESTS=1 node --experimental-strip-types --no-warnings --import ./tests/setup.ts --test tests/security/**/*.test.ts",
    "test:credproxy": "cd tests/integration/credproxy && docker compose run --rm claude; docker compose down -v",
    "test:proxy": "cd tests/integration/proxy && docker compose run --rm test; docker compose down -v"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@slack/bolt": "^4.6.0",
    "@types/dockerode": "^4.0.1",
    "@types/ws": "^8.18.1",
    "dockerode": "^4.0.9",
    "ws": "^8.19.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/tar-stream": "^3.1.4",
    "tar-stream": "^3.1.7"
  }
}
